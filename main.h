#include <stdio.h>

// Function Calls
void printMenu();
void rotate(unsigned int userInput);
int parity(unsigned int userNum);
long long int endian(long long int usrInput);
void clz(unsigned int val);
